<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hotmail.fignunes.btg">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:name=".MobileChallenge"
            android:allowBackup="true"
            android:icon="@drawable/ic_logo_btg"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".presentation.currencies.CurrenciesActivity"
                  android:label="@string/list_of_currencies"/>
        <activity android:name=".presentation.quotedollar.QuoteDollarActivity"
                  android:windowSoftInputMode="stateHidden"
                  android:label="@string/currency_converter"/>
        <activity android:name=".presentation.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>